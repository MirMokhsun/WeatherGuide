import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  txtView: {
    flex: 5,
    paddingTop: 10,
  },
  txt: {
    color: 'white',
    fontSize: 18,
    paddingStart: 10,
  },
});

export default styles;
